import { RegisterForm } from '../../components/Auth';

function Register() {
  return (
    <div className="container register-page w-100 mt-auto">
      <RegisterForm />
    </div>
  );
}

export default Register;
